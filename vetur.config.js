module.exports = {
  projects: [
    {
      root: "./front_end", // root of your vue project (should contain package.json)
      package: "./package.json", // Relative to root property, don't change this.
      jsconfig: "./jsconfig.json", // Relative to root property, don't change this.
    },
  ],
};
